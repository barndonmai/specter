"""
Specter skills.

The on-disk folder names use hyphens to match OpenClaw's skill discovery
convention (skills/<name>/SKILL.md). For Python imports, both this package
and the per-skill subfolders also expose underscore aliases via sys.path
when needed — but for reliable imports, use the helper invocation form
documented in each SKILL.md.
"""
